# Claude Code 빌드 지침서 — DNF Market Analyst

> PRD(DNF-Market-Analyst_PRD.md)를 실행 단위로 변환한 작업 지침이다.
> 두 문서를 프로젝트 루트에 두고 각 Phase 시작 시 모두 참조할 것.

---

## 0. 절대 규칙 (꼭 지켜야 할 사항 — 위반 시 해당 작업 무효)

1. **자율 해결**: 에러·의존성·빌드 문제는 스스로 진단·해결한다. 해결 후 "무엇이 문제였고
   어떻게 고쳤는지" 비개발자가 이해할 한 문장으로 보고한다. 기술적 질문을 사용자에게
   넘기지 않는다.
2. **검수 게이트**: 각 Phase 완료 시 멈추고 사용자 확인 후 진행한다. 게이트 생략 금지.
3. **실데이터 원칙**: 시세·이벤트 데이터를 절대 지어내지 않는다. 수집 실패·결손은 공백과
   실패 로그로 정직하게 남긴다. 데모용 가짜 스냅샷 생성 금지. (UI 개발 중 임시 목데이터는
   `dev/` 폴더에 격리하고 최종 산출물에 혼입 금지)
4. **API 매너**: 네오플 오픈 API 이용안내·약관·BI 가이드를 Phase 0에서 정독하고 요약을
   보고한다. 호출 간 최소 대기 시간을 두고, 정책상 허용량을 절대 초과하지 않는 보수적
   설계를 한다. 지원 회사의 API다 — 호출 로그가 곧 인상이다.
   재시도는 **총 2회, 간격 30초·120초 백오프**까지 허용한다(2026-08-27 개정).
   단 **앞선 5개 품목이 두 엔드포인트 모두 5xx면 남은 재시도를 중단하고 실패 처리**한다.
   점검처럼 서버가 통째로 내려간 구간에서 무의미한 호출을 쌓지 않기 위한 조건이다.
5. **키 보안**: API 키·Anthropic 키는 .env와 GitHub Secrets로만 관리. .gitignore 확인,
   커밋 전 키 문자열 스캔. 코드·로그·에러 메시지에 키 노출 금지.
6. **IP·표기**: 던파 로고·아트워크·스크린샷 사용 금지. 아이템 아이콘은 API가 반환하는
   공식 이미지 URL만 사용. 푸터와 README에 "Neople 오픈 API 사용" 고지(BI 가이드 준수)와
   "비공식 팬메이드 포트폴리오, 네오플과 무관" 고지를 병기한다.
7. **AI 해석의 정직성**: 원인 가설은 근거 URL과 신뢰도(확정/추정) 없이 출력하지 않는다.
   근거를 못 찾으면 "원인 미상, 관찰 지속"으로 쓴다. 그럴듯한 단정 생성 금지.
8. **Git**: 코드 커밋·푸시는 사용자가 직접 한다. 예외는 GitHub Actions 봇의 **데이터 파일
   커밋(data/ 경로 한정)** 뿐이다. Actions 워크플로우가 data/ 외 경로를 커밋하지 않도록
   경로를 명시적으로 제한한다.
9. **비용 통제**: LLM 호출은 이상 탐지된 항목에만 수행(전 품목 호출 금지). 브리핑 1회당
   비용을 로그로 남기고, 일 상한($0.3) 초과 시 해당 실행을 중단하고 보고한다.
10. **보고 언어**: 모든 보고·주석·커밋 메시지 한국어. UI 기본 언어 한국어.

### §0 개정 이력

- **2026-08-27 — 규칙 4 재시도 정책 완화.** 재시도 1회 → 총 2회(30초·120초 백오프).
  단 앞선 5개 품목이 두 엔드포인트 모두 5xx면 남은 재시도를 중단하고 실패 처리한다.
  계기: 2026-08-27 02시대(실제 실행 04:46) 회차가 전 품목 HTTP 503으로 실패했고,
  같은 날이 목요일 정기점검일로 확인됐다. 1회 재시도로는 짧은 서버 불안정도 못 넘기고,
  무제한 재시도는 점검 구간에서 호출만 쌓는다. 두 문제를 함께 막는 절충안이다.
  코드 반영: `pipeline/collect.py` (RETRY_LIMIT·RETRY_BACKOFF_SEC·OUTAGE_PROBE_ITEMS).

---

## 1. 프로젝트 구조

```
dnf-market-analyst/
├── pipeline/
│   ├── collect.py            # 스냅샷 수집 (PL-1)
│   ├── aggregate.py          # 시계열 병합 (PL-2)
│   ├── detect.py             # 이상 탐지 (PL-3)
│   ├── interpret.py          # AI 맥락 해석 (PL-4)
│   ├── briefing.py           # 데일리 브리핑 (PL-5)
│   ├── run_daily.py          # 심야 통합 실행
│   └── prompts/              # 해석·브리핑 프롬프트
├── config/
│   ├── items.json            # 추적 품목 (id, 이름, 카테고리, 선정 사유)
│   ├── thresholds.json       # 이상 탐지 임계치
│   └── events.csv            # 이벤트·패치 수동 큐레이션
├── data/                     # Actions 봇이 커밋하는 유일한 경로
│   ├── snapshots/
│   ├── timeseries.json
│   ├── anomalies.json
│   └── briefings.json
├── .github/workflows/
│   ├── snapshot.yml          # cron 하루 3회 수집
│   └── daily.yml             # 심야 분석+브리핑
├── dashboard/                # Vite + React + Tailwind
└── README.md
```

## 2. Phase별 작업

### Phase 0 — 셋업·정책 확인 (게이트 0)
- 저장소·구조·venv·dashboard 셋업, .env.example 작성 (NEOPLE_API_KEY, ANTHROPIC_API_KEY)
- developers.neople.co.kr에서 이용안내·결과 코드·약관·BI 가이드 정독 → 호출 제한,
  표기 의무, 금지 사항을 요약 보고
- 경매장 검색·시세·판매완료·아이템 검색 엔드포인트를 실키로 1회씩 테스트 호출,
  실제 응답 필드를 확인해 스키마 확정 (문서와 다르면 실응답 기준)
- 품목 후보 40~60종 목록 작성 (카테고리·선정 사유 포함)
- **게이트 0 보고**: 정책 요약 + 실응답 샘플 + 품목 후보 → 사용자가 품목 확정

### Phase 1 — 수집기·스케줄러 가동 (게이트 1, 최우선)
- collect.py: itemIds 일괄 조회 활용, 호출 간 대기, 실패 재시도 2회(30초·120초 백오프,
  앞선 5개 품목 전부 5xx면 조기 중단), 부분 실패 허용
- snapshot.yml: cron 5회(KST 07:17/11:17/15:17/19:17/23:17), 심야 회차(02:17)는 daily.yml.
  cron 분은 정각을 피해 17분 고정(정각은 스케줄러 혼잡으로 드롭·지연이 잦다).
  data/ 한정 커밋, 실패 시 로그 남김
- aggregate.py: 스냅샷 → timeseries 병합 (슬롯 중복 방지)
- **게이트 1 보고**: 스냅샷 3회 연속 정상 수집 확인 후 통과. 이후 Phase는 데이터가
  쌓이는 동안 병행 진행

### Phase 2 — 분석·브리핑 (게이트 2)
- detect.py: 전일 대비/이동평균 이탈/매물 급변, 임계치 config 관리, 데이터 7일 미만
  구간은 전일 대비만 적용
- interpret.py: 이상 항목 ±3일 events 교차 → 가설 생성 (절대 규칙 7 준수).
  events.csv 초기 데이터는 최근 1개월 공지·이벤트를 사용자가 확인할 수 있게 후보로
  정리해 게이트에서 제시
- briefing.py: 헤드라인+3줄 요약+주목 변동. 이상 0건이면 "안정 구간" 브리핑
- daily.yml: 심야 슬롯에서 수집→탐지→해석→브리핑 통합 실행
- **게이트 2 보고**: 실데이터 기반 샘플 브리핑 2건 + 가설의 근거 링크 제시 → 검수

### Phase 3 — 대시보드 (게이트 3)
- 4화면: 마켓 오버뷰 / 아이템 상세(이벤트 마커 오버레이 차트) / 브리핑 아카이브 /
  방법론·데이터 정책
- 디자인 토큰(§3) 적용. 차트는 등록가·실거래가 병기, 결손일 공백 표기
- 반응형(640px 1열), 스켈레톤·빈 상태, 딥링크(/item/:id)
- **게이트 3 보고**: Playwright 스크린샷 (데스크톱/모바일) + 수치 정합성 검증

### Phase 4 — 배포·문서화 (최종 게이트)
- Vercel 배포 (Actions 데이터 커밋 → 자동 재배포 확인)
- README: 컨셉 한 줄(조회 도구가 아니라 분석 시스템) → 아키텍처 → 수집·이상탐지 기준
  공개 → 품목 선정 근거 → 비용 실측 → API 출처·팬메이드 고지
- 최종 검수: 콘솔 오류 0, Lighthouse, 7일 연속 브리핑 확인은 운영 경과 후 별도 보고

## 3. 디자인 토큰 (라이트 데이터 대시보드 — 이전 프로젝트와 차별화)

```css
:root {
  --bg-base: #F7F8FA;      /* 페이지 배경 */
  --bg-surface: #FFFFFF;   /* 카드 */
  --border: #E3E7EE;
  --text-primary: #1B2536; /* 딥 네이비 */
  --text-secondary: #5B667A;
  --text-muted: #93A0B4;
  --accent: #2450A8;       /* 포인트 블루 */
  --up: #D64545;           /* 상승 (국내 금융 관례: 레드) */
  --down: #2E6BD6;         /* 하락 (블루) */
  --neutral: #8A93A6;
  --warn: #C8891A;         /* 이상 변동 하이라이트 */
}
```
- 폰트: Pretendard, 숫자는 tabular-nums 모노스페이스
- 카드 radius 10px, 그림자 미세하게(과한 섀도 금지), 본문 14px+, 대비 WCAG AA
- 차트: 라인 심플, 이벤트 마커는 --warn 세로 점선 + 호버 툴팁
- 등락 표기는 색+부호(▲▼) 병기 (색맹 대응)

## 4. 프롬프트 설계 (pipeline/prompts/)
- interpret_system.txt: 역할(게임 시장 분석가), 입력(변동 지표+후보 이벤트 목록),
  출력 JSON 스키마, 규칙("제공된 이벤트 외 원인 창작 금지, 시점 근접만으로 인과 단정
  금지 — '시점 일치' 수준으로 서술, 근거 없으면 원인 미상"), 신뢰도 기준 정의
- briefing_system.txt: 분석가 데일리 노트 톤(간결·수치 중심·과장 금지), 3줄 요약 규칙

## 5. 게이트 보고 형식
1. 완료 체크리스트 / 2. 발생 문제와 해결(각 한 문장) / 3. 실측치(수집 건수·API 호출 수·
LLM 비용) / 4. 다음 Phase 진행 여부 질문. 사용자 응답 없이 다음 Phase 착수 금지.
