# DNF Market 비주얼 업그레이드 지침서

이 문서는 DNF Market 저장소 전용입니다. 저장소 루트에 넣고 Claude Code에 "이 문서를 읽고 Phase 1부터 시작해"라고 지시하면 됩니다.

목표: 시세 대시보드를 "실시간 관제 화면"처럼 보이게 만든다. 데이터 신뢰감이 최우선이므로 화려함보다 정돈된 반응성에 집중한다.

사용 라이브러리는 두 가지다.

| 라이브러리 | 역할 |
|---|---|
| Bklit UI | 차트 컴포넌트 (라인, 바, 영역, 링 등) |
| Motion (`motion`) | UI 인터랙션. 카드 등장, 호버 반응, 숫자 카운트업, 내용 전환 |

---

## 반드시 지킬 것 (공통 원칙)

1. **하나의 요소에 두 라이브러리 효과를 겹쳐 쓰지 않는다.**
2. **애니메이션은 절제한다.** 화면당 핵심 연출 1~2개면 충분하다. 모든 요소가 움직이면 채용 담당자가 데이터에 집중할 수 없다.
3. **움직임 최소화 설정을 존중한다.** 사용자 시스템에 "동작 줄이기" 설정이 켜져 있으면 애니메이션을 생략하거나 즉시 완료 상태로 보여준다. (Motion은 자동 지원)
4. **성능 기준: 배포 후 모바일에서도 스크롤이 끊기지 않아야 한다.** 끊기면 해당 연출을 단순화하거나 제거한다.
5. **디자인 톤 유지.** 기존의 다크 톤과 Pretendard 타이포그래피(제목 ExtraBold, 소제목 SemiBold, 본문 Regular)를 그대로 따르고, 라이브러리 기본 색상이나 기본 폰트가 화면에 노출되지 않게 한다.
6. **문구 규칙 유지.** 화면에 새로 추가되는 모든 문구는 쉬운 용어로 쓰고, 줄표(—, –)와 하이픈 연결을 쓰지 않는다.
7. **GitHub Actions 자동 수집과 데이터 갱신 흐름에 영향을 주지 않는다.** 화면 표현만 바꾸고 수집·저장 로직은 건드리지 않는다.

## 설치

```bash
# Motion (구 Framer Motion)
npm install motion

# Bklit UI (shadcn 레지스트리 방식, shadcn 초기화가 안 되어 있다면 init 먼저)
npx shadcn@latest init
npx shadcn@latest add @bklit/line-chart
```

Bklit UI는 비교적 새로운 라이브러리이므로, 컴포넌트를 추가하기 전에 반드시 공식 문서(bklit.com 또는 github.com/bklit/bklit-ui)에서 현재 제공되는 컴포넌트 이름과 설치 명령을 확인한 뒤 진행한다. 문서와 다르게 동작하면 추측으로 고치지 말고 문서 기준으로 다시 확인한다.

참고용 기본 코드:

```tsx
// Motion: 화면에 들어올 때 카드 등장
import { motion } from "motion/react";

<motion.div
  initial={{ opacity: 0, y: 24 }}
  whileInView={{ opacity: 1, y: 0 }}
  viewport={{ once: true, amount: 0.3 }}
  transition={{ duration: 0.5, ease: "easeOut" }}
>
  {/* 카드 내용 */}
</motion.div>
```

---

## Phase 1. Bklit UI 차트 교체

1. 현재 대시보드의 주요 차트를 확인하고, Bklit UI로 교체했을 때 이득이 있는 차트만 선별한다. (교체 기준: 기존보다 시각적으로 명확해지고, 툴팁과 애니메이션 품질이 올라가는 경우에만 교체)
2. 시세 추이는 라인 차트 또는 영역 차트, 품목 간 비교는 바 차트, 비중 표시는 링 차트를 사용한다.
3. 차트 색상은 기존 대시보드 팔레트에 맞춰 직접 지정한다. 라이브러리 기본 색을 그대로 두지 않는다.
4. 교체 후 기존 차트에서 쓰던 기능(툴팁 정보량, 축 라벨, 반응형 크기 조절)이 하나도 빠지지 않았는지 화면별로 비교 확인한다. 빠지는 기능이 있으면 그 차트는 교체하지 않고 원래 것을 유지한다.

## Phase 2. Motion 인터랙션

1. 첫 로드 시 지표 카드들이 위에서 아래 순서로 0.05초 간격씩 부드럽게 등장하게 한다. (한 번만 실행, 새로고침 시 재실행)
2. 핵심 지표 숫자(시세, 변동률 등)에 카운트업 효과를 넣는다. 소수점과 천 단위 콤마 형식은 기존 표기 그대로 유지한다.
3. 카드 호버 시 살짝 떠오르는 효과(위로 2~4px, 그림자 강화)를 넣는다. 클릭 요소에만 적용하고 단순 정보 카드에는 넣지 않는다.
4. 데일리 브리핑 영역은 내용이 갱신될 때 이전 내용이 사라지고 새 내용이 자연스럽게 나타나는 전환을 넣는다.
5. 이상 변동 알림처럼 주목해야 하는 요소에는 등장 시 1회만 강조 효과(색 펄스 또는 스케일 강조)를 준다. 반복 깜빡임은 금지한다.

## Phase 3. 검수 게이트

아래 항목을 모두 확인한 후 스크린샷과 함께 보고한다.

- [ ] 모든 차트가 데스크톱과 모바일에서 깨지지 않는다
- [ ] GitHub Actions 자동 수집과 데이터 갱신 흐름에 영향이 없다
- [ ] 첫 화면 로딩이 기존보다 체감상 느려지지 않았다
- [ ] 애니메이션이 데이터 판독을 방해하지 않는다
- [ ] Vercel 미리보기 배포에서 정상 동작한다

---

## 실행 방식

1. Phase 단위로 진행하고, 한 Phase가 끝나면 반드시 배포 미리보기 링크나 스크린샷으로 검수받은 뒤 다음 Phase로 넘어간다.
2. 오류가 나면 스스로 해결하되, 라이브러리 문서 확인이 필요한 경우 추측하지 말고 공식 문서를 조회해서 해결한다.
3. 작업 완료 후에는 무엇이 바뀌었는지 화면 기준으로 한 줄씩 요약해서 보고한다.

## 넣지 말아야 할 것 (금지 목록)

- 자동 재생되는 무한 반복 애니메이션 (로딩 표시 제외)
- 마우스를 따라다니는 커서 효과
- 페이지 전체를 덮는 화면 전환 연출
- 소리
- 3초 이상 걸리는 인트로
- 데이터 수치를 가리거나 늦게 보여주는 연출
